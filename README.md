# 🎲 Galton Board - D20 Dice Randomness Simulator

3D симулятор доски Гальтона для проверки случайности 20-гранных кубиков (d20). Проект использует реалистичную 3D физику для симуляции падения кубиков через препятствия и сбора статистики результатов.

## ✨ Возможности

- **3D физика в реальном времени** - Использует Three.js для рендеринга и Cannon-es для физической симуляции
- **Доска Гальтона** - Кубики падают через систему препятствий (pegs) для случайного распределения
- **D20 кубики** - Реалистичная симуляция 20-гранных кубиков с определением выпавшего числа
- **Статистика в реальном времени** - Отслеживание и визуализация распределения результатов
- **Тестирование случайности** - Графики для анализа равномерности распределения

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера (откроется http://localhost:3000)
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен сборки
npm run preview
```

**Важно:** Исходный `index.html` в корне проекта **нельзя** открывать напрямую в браузере. Используйте `npm run dev` для разработки или открывайте `dist/index.html` после сборки.

## 🎮 Управление

- **Add Dice** - Добавить один кубик
- **Add 10 Dice** - Добавить 10 кубиков
- **Reset Stats** - Сбросить статистику
- **Clear Dice** - Удалить все кубики с доски

### Горячие клавиши
- `Space` - Добавить кубик
- `R` - Сбросить статистику
- `C` - Очистить доску

## 🏗️ Архитектура

Проект разделен на модули:

- **scene.js** - Настройка Three.js сцены, камеры и освещения
- **physics.js** - Физический движок Cannon-es с оптимизированным SAPBroadphase
- **dice.js** - Класс D20 кубика с 3D моделью и физикой
- **galtonBoard.js** - Доска Гальтона с препятствиями и стенками
- **stats.js** - Система статистики и визуализации с инкрементальным обновлением DOM
- **main.js** - Главный файл, связывающий все компоненты
- **config.js** - Централизованная конфигурация с константами
- **utils.js** - Утилитарные функции для цветов и расчетов

## 👨‍💻 Для разработчиков

### Режимы работы

**Development Mode (npm run dev)**
- Использует Vite dev сервер на порту 3000
- Поддерживает hot module replacement (HMR)
- Отслеживание ошибок в реальном времени
- Включает source maps для отладки

**Production Mode (npm run build)**
- Собирает оптимизированный бандл в папку `dist/`
- Минифицирует JS/CSS код
- Создаёт чанки для Three.js и Cannon-es
- `dist/index.html` можно открыть напрямую в браузере

### Почему нельзя открыть index.html напрямую?

Исходный `index.html` содержит ES модули (`type="module"`), которые:
- Блокируются CORS политикой при открытии через `file://`
- Требуют HTTP/HTTPS сервер для работы

Решения:
1. **Разработка:** Используйте `npm run dev` (запускает HTTP сервер)
2. **Продакшен:** Соберите проект `npm run build` и откройте `dist/index.html`

### Структура проекта

```
planko_dice/
├── src/
│   ├── main.js           # Точка входа приложения
│   ├── scene.js          # Three.js сцена, камера, освещение
│   ├── physics.js        # Cannon.es физический движок
│   ├── dice.js           # D20 кубик с 3D моделью
│   ├── galtonBoard.js    # Доска Гальтона с pegs
│   ├── stats.js          # Статистика и визуализация
│   ├── config.js         # Конфигурация проекта
│   ├── utils.js          # Утилитарные функции
│   └── styles.css        # Стили приложения
├── dist/                 # Сборка (создаётся npm run build)
│   ├── index.html        # Готовый к деплою HTML
│   └── assets/           # JS и CSS файлы
├── index.html            # Исходный HTML (только для dev)
├── package.json          # Зависимости и скрипты
├── vite.config.js        # Конфигурация Vite
└── README.md             # Этот файл
```

### Сборка и деплой

```bash
# Сборка для продакшена
npm run build

# Предпросмотр собранной версии
npm run preview

# Деплой на GitHub Pages (настроен через .github/workflows/deploy.yml)
git push origin main
```

### Настройка конфигурации

Все магические числа вынесены в `src/config.js`:

```javascript
export const CONFIG = {
    physics: { gravity, iterations, tolerance, ... },
    dice: { maxDice, radius, mass, ... },
    galtonBoard: { rows, pegRadius, spacing, ... },
    colors: { ... },
    camera: { fov, position, ... }
};
```

### Оптимизации производительности

**Реализованные:**
- SAPBroadphase вместо NaiveBroadphase (O(n log n) vs O(n²))
- Кэширование материалов в physics.js
- Инкрементальное обновление DOM в stats.js
- Вычисление результата только при остановке кубика

**Требуется Node.js:**
- Для разработки (npm, Vite dev сервер)
- Для сборки (npm run build)
- **Не требуется** для работы готового приложения (dist/)

## 📊 Статистика

Панель статистики показывает:

- Общее количество бросков
- Частоту выпадения каждого числа (1-20)
- Процентное распределение
- График с ожидаемой частотой (5% для идеального d20)
- Цветовую индикацию отклонения от ожидаемого значения

## 🎯 Цель проекта

Этот симулятор создан для визуального и статистического анализа случайности при использовании физической симуляции для генерации случайных чисел. Доска Гальтона добавляет дополнительный элемент случайности через взаимодействие кубиков с препятствиями.

## 🔧 Технологии

- **Three.js** - 3D графика и рендеринг
- **Cannon-es** - Физический движок
- **Vite** - Сборщик и dev-сервер
- **GitHub Actions** - CI/CD для автоматической публикации на GitHub Pages

## 📋 Код-стандарты

**Принципы:**
- **DRY** - Использовать утилитарные функции вместо дублирования
- **KISS** - Простой и понятный код
- **Конфигурация через объекты** - Все настройки в `config.js`
- **Валидация** - Проверка существования DOM-элементов
- **Оптимизация** - Кэширование, инкрементальные обновления

**Соглашения:**
- Имена классов: `PascalCase`
- Имена функций/переменных: `camelCase`
- Константы: `UPPER_SNAKE_CASE` (в config.js)
- ES6+ синтаксис (модули, async/await, деструктуризация)

## 📝 Лицензия

MIT License - см. файл LICENSE

## 🌐 Demo

После настройки GitHub Pages, проект будет доступен по адресу:
`https://yourusername.github.io/planko_dice/`

---

Сделано с ❤️ для изучения случайности и физической симуляции